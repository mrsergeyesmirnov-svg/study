{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci && npm run build -w @vtgshmot/api"
  },
  "deploy": {
    "startCommand": "npm run start:railway -w @vtgshmot/api",
    "restartPolicyType": "ON_FAILURE"
  }
}
